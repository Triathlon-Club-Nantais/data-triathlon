# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/
*.egg-info/
.benchmarks/
.pytest_cache/

# Database
*.db
*.sqlite3

# Excel / rapports de fiabilité (données personnelles ou générées localement)
*.xlsx
*.xls
xlsx_urls.json
audit_report.md
reliability_report.json
reliability_report.md
bc_pagination_report.json

# Node
node_modules/
dist/
.env.local
.env.production.local

# Port du backend de dev, publié par backend/scripts/dev_server.py — propre à chaque worktree
.dev-backend.json

# Fichier de version écrit par le buildCommand Render (#134). Contient le tag
# git courant, lu par `app/version.py`. Jamais commité — c'est un artefact de build.
backend/VERSION

# Site de documentation généré par Jekyll (docs/) — jamais commité, le workflow
# Pages le reconstruit à chaque déploiement
_site/
.jekyll-cache/
.jekyll-metadata

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# AI Tools
.claude/memory/
docs/local/
# État persistant du skill /onboard (voir .claude/skills/onboard/SKILL.md)
.claude/skills/onboard/state.json
# Worktrees (git worktree fallback)
.claude/worktrees/

# Worktrees isolés (superpowers)
.worktrees/
# Ledger d'exécution de subagent-driven-development — jetable, un dossier par plan
.superpowers/
